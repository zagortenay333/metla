fn print (str: Slice(U8)) {
    import "%stdlib/linux";
    linux.write(1, str.data, str.count);
}

fn println (str: Slice(U8)) {
    print(str);
    print("\n");
}

fn: #ir1 main {
    var i = 1;
    var n = 10;
    while i < n {
        println("Hello World!");
        i += 1;
    }
}
